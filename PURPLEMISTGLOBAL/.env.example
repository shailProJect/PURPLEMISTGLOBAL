# Vite exposes only variables prefixed with VITE_ to the client.
# Copy this file to .env and set your backend URL.
# This should point at the Express server in /server (see server/.env.example).
VITE_BACKEND_URL=http://localhost:5000
